import styled from "styled-components";
import { ResponsiveContainer as Wrapper } from "recharts";

export const ResponsiveContainer = styled(Wrapper)`
  background: #fff;
  font: normal 400 12px Jost;
`;
